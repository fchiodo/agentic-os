<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "https://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- PyInstaller one-file extracts its private, pre-signed Python/MLX runtime
       before launch. Hardened Runtime must permit those bundled libraries;
       the sidecar still accepts only the verified local model and uses no
       network during conversion. -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
